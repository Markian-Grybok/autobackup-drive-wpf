<Window x:Class="AppCopyDirecToDrive.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="About CopyDirecToDrive" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FFEEEEEE">
        <Border Margin="15" Background="White" Padding="20" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="LightGray" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="About CopyDirecToDrive" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 10 20"/>
                <TextBlock FontSize="14" TextWrapping="Wrap" Margin="0 0 0 20">
                    <Run Text="Owner: Markian Grybok"/>
                    <LineBreak/>
                    <Run Text="Email: markiian.hrybok.ki.2022@lpnu.ua"/>
                    <LineBreak/>
                    <Run Text="Program Version: 1.0.0"/>
                    <LineBreak/>
                    <Run Text="© 2025 CopyDirecToDrive. All rights reserved."/>
                </TextBlock>
                <Button x:Name="btnClose" Content="Close" HorizontalAlignment="Center" Width="179" BorderThickness="10" Click="Close_Click" FontSize="10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>